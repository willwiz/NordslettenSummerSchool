import numpy as np
from biomechanics import *


def main():
    # Define time domain
    time = np.linspace(0, 10, 101, dtype=f64)
    # Make
    fiber_direction = np.array([1, 0, 0], dtype=f64)
    sheet_direction = np.array([0, 1, 0], dtype=f64)
    #


if __name__ == "__main__":
    main()
